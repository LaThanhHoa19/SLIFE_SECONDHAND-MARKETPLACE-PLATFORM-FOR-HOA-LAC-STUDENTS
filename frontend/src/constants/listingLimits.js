/**
 * Khớp backend ListingImageService (config key MAX_IMAGES_PER_POST, default 10).
 * Nếu admin đổi cấu hình server, có thể sau này lấy từ API public config.
 */
export const MAX_IMAGES_PER_LISTING = 10;
